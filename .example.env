# Example configuration for EzyGitSync
# GIT_URL: repository URL (HTTPS), e.g. https://github.com/owner/repo.git or custom git server URL
# GIT_USERNAME and GIT_PASSWORD are optional (for private repos). If using a token, set it as GIT_PASSWORD.
# REPO_PATH: local path where the repository should be cloned/pulled. Relative to project root or absolute path.
# APP_USER: username for single-user auth
# APP_PASS_HASH: hashed password (created by setup). You can also provide a plain password in setup to create the hash.
# LOG_FILE: path to log file (default logs/app.log)

GIT_URL=
GIT_USERNAME=
GIT_PASSWORD=
REPO_PATH=repos/myrepo
APP_USER=admin
APP_PASS_HASH=
LOG_FILE=logs/app.log
# Comma-separated list of paths to protect from clear/remove operations
# Relative paths are resolved against project root
PROTECT_PATHS=logs,.env
